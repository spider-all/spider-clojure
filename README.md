# spider-clojure

Crawl GitHub APIs and store the discovered orgs, repos, commits...

### Run in terminal

``` bash
lein run
```

Build fat jar

``` bash
lein uberjar
```

### Run in docker

``` bash

```
